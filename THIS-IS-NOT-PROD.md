# ⚠️ This checkout is NOT production

Production is **`C:\Users\banjigum1\Desktop\YT-Scraper\YSXXS`** — always confirm with:

```powershell
nssm get ysx-backend AppDirectory
```

This Documents checkout is a working copy for development sessions. Deploying means: commit + push here → pull + build + restart in the production repo (see `docs/RECOVERY.md` §4 and `HANDOFF.md`).
