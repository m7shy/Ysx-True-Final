# Dev-mode only (npm run dev). Empty VITE_API_URL makes services/apiClient.ts
# use relative URLs, so the Vite proxy in vite.config.ts forwards /api to the
# local backend same-origin (the backend's CORS is pinned to the prod origin).
# `vite build` runs in production mode and does not read this file.
VITE_API_URL=
